public void addLast(int data) {
 Node last = head; 
 while (last.next != null) { 
 last = last.next;
 }
 Node node = new Node(data); 
 node.next = null; 
 last.next = node; 
}
llist.addLast(300);
llist.printList();
